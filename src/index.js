import { createResourceSync, ResourceModel, ResourceCollection, resource } from './resource'
import Storage from './storage'

export { createResourceSync, ResourceCollection, ResourceModel, Storage, resource }
